steps:
- name: 'gcr.io/cloud-builders/gcloud'
  id: Update staging server
  entrypoint: /bin/sh
  args:
  - '-c'
  - gcloud compute ssh vm-created-by-deployment-manager --zone=us-central1-f --tunnel-through-iap --command='/bin/sh /home/mmc/pull.sh'