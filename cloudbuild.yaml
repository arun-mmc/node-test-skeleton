steps:
- name: 'gcr.io/cloud-builders/gcloud'
  id: Update staging server
  entrypoint: /bin/sh
  args:
  - '-c'
  - gcloud compute ssh vm-created-by-deployment-manager --zone=us-central1-f  --command='/bin/sh cd /home/mmc/pull.sh'